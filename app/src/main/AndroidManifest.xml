<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="blind.al.fileguardplugin">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application android:allowBackup="true">
        <service android:enabled="true" android:name="blind.al.fileguardplugin.PluginMain" android:exported="true" android:process=":remote">

            <intent-filter>
                <action android:name="blind.al.fileguardplugin.PluginMain"></action>
            </intent-filter>
        </service>
    </application>

</manifest>
